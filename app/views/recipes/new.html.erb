<%= form_for @recipe do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :ingredients %>
  <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name do |ing|%>
    <%= ing.check_box %>
    <%= ing.label %>
  <% end %>
  <%= f.submit %>
<% end %>
